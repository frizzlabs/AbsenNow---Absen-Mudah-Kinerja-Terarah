export interface Employee {
  id: string;
  userId: string;
  firstName: string;
  lastName: string;
  avatarUrl?: string;
  position: string;
  department: string;
  joinDate: string;
  phoneNumber?: string;
  address?: string;
}
